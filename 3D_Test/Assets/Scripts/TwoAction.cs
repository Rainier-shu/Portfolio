using UnityEngine;

public class TwoAction : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Item two = new Item();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
